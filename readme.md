### Demo app to showcase deck.gl

## Get Started

* Install dependencies
```
yarn install  
```
* Add MAPBOX_TOKEN in .env file
    ```
    REACT_APP_MAPBOX_TOKEN=< Add Mapbox Token here >
    ```

* Run project
  
```
yarn start
```